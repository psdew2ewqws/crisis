fastapi==0.115.6
uvicorn[standard]==0.34.0
psycopg[binary]>=3.2.10
psycopg_pool==3.3.1
python-dotenv==1.0.1
networkx==3.4.2
pydantic==2.10.4
openpyxl==3.1.5
requests>=2.31.0
beautifulsoup4>=4.12.0
# live Middle East RSS crisis-signal aggregator (in-memory; news_rss.py)
feedparser>=6.0.0
# optional — the Deer Graph flow & Mesa simulation degrade gracefully if absent
langgraph==0.2.60
mesa==3.1.1
# agent-based sim calibration (data-driven fit; always-on)
pandas>=2.0
scipy>=1.10
# DoWhy causal robustness layer — active when VALIDATE_CAUSAL=1 in .env
dowhy>=0.14

pinecone>=5.0
